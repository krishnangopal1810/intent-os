# Domain Spec: YouTube

YouTube remains the first concrete behavior domain. The current implementation
supports local fixture classification into learning, entertainment, or unknown.

The generic `ActivityEvent` classifier should treat YouTube as one surface among
many while preserving the YouTube MVP CLI and evaluation path.
